machine:
  node:
    version: 6.8.1

dependencies:
  override:
    - npm i -g markdown-to-graphcool

deployment:
  master:
    branch: /^(?!(dev|master)$).*/
    commands:
      - ./scripts/deploy.sh:
